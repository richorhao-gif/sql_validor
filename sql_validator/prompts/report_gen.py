"""
prompts/report_gen.py
──────────────────────
report_generator 节点使用的 Prompt 模板（生成最终 MD 报告）。
"""

CHANGE_REPORT_PROMPT = """\
请根据以下结构化数据，生成一份专业的 Markdown 格式「变更说明对比报告」。

报告数据（JSON）：
{report_data}

报告结构要求（按此顺序）：
1. 标题：`# SQL 脚本变更说明对比报告`
2. 元信息表格（生成时间、文件数、总体判定、风险等级）
3. 执行概要（用一段话概述本次发版的整体情况和审核结论）
4. 变更覆盖情况（三栏表格：✅ 已确认 / ❌ 遗漏 / ⚠️ 额外变更）
5. 高风险操作清单（如无则注明"无高风险操作"）
6. 数据库变更影响详情
   - 新增对象列表
   - 修改对象列表（说明修改内容）
   - 删除对象列表
   - 数据变更影响（哪些表有数据变动）
7. 执行顺序与依赖关系说明
8. 审核结论与建议（明确的发版建议：可以发版 / 需整改后发版 / 禁止发版）

格式规范：
- verdict=PASS 用 ✅，WARN 用 ⚠️，FAIL 用 ❌
- 高风险操作用 🔴 标注
- 使用 Markdown 表格、代码块、粗体来提升可读性
- 输出纯 Markdown 文本，不要 ```markdown 包裹，不要额外解释
"""

SYNTAX_REPORT_PROMPT = """\
请根据以下结构化数据，生成一份专业的 Markdown 格式「SQL 脚本语法与质量分析报告」。

报告数据（JSON）：
{report_data}

报告结构要求（按此顺序）：
1. 标题：`# SQL 脚本语法与质量分析报告`
2. 质量评分概要（评分/文件数/ERROR数/WARNING数/INFO数，用大字体或表格突出评分）
3. 各文件质量概览表格（文件名 | ERROR | WARNING | INFO | 主要问题）
4. 🔴 ERROR 级问题明细（按文件分组，每条含规则名、描述、涉及SQL片段、修复建议）
5. 🟡 WARNING 级问题明细（同上）
6. 🔵 INFO 级问题明细（可折叠或简化展示）
7. 专项分析
   - 安全风险专项（无 WHERE 的 DML、TRUNCATE、DROP）
   - 性能规范专项（SELECT *、缺失索引提示等）
8. 改进建议清单（编号列表，针对本次发版的具体可操作建议）

格式规范：
- 使用 Markdown 表格和代码块
- 高危问题用 🔴 标注，中等用 🟡，低级用 🔵
- 输出纯 Markdown 文本，不要额外解释
"""


SYNTAX_FILE_ANALYSIS_PROMPT = """\
你是 SQL 代码审查专家。请分析以下单个 SQL 文件的语法问题，针对此文件精简输出提炼和建议。

文件名：{filename}
语句数量：{statement_count}

静态分析工具已发现的问题（JSON）：
{issues_json}

主要语句摘要：
{statements_summary}

请输出：
- top_issues：最重要的 3～5 条问题摘要，每条 ≤ 50 字
- recommendations：针对此文件的2～3 条改进建议，每条 ≤ 80 字
"""
