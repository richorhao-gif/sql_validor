# SQL 脚本变更说明对比报告

| 项目 | 内容 |
|------|------|
| **生成时间** | 2026-05-02T16:57:10.960428 |
| **脚本文件数** | 3 |
| **总体判定** | ⚠️ WARN |
| **风险等级** | MEDIUM |

## 执行概要

本次发版涉及 3 个 SQL 脚本，主要完成用户等级维度表扩展（新增 `discount_rate` 字段并回填历史数据）、创建用户日汇总事实表 `fact_user_daily_summary` 及 3 个索引、创建用户等级营收视图 `vw_user_level_revenue`。变更说明与脚本整体对应，无缺失条目，但存在 **2 个风险点**：视图 `vw_user_level_revenue` 声称引用 `discount_rate` 字段，但实际定义中可能未使用该字段，存在描述不一致；此外 `fact_orders` 的 `level_id` 字段允许为空，可能影响视图聚合结果。另脚本中创建了 3 个索引，变更说明未提及，属于额外变更，但风险较低。综合判定为 **WARN**，风险等级 **MEDIUM**。

## 变更覆盖情况

| ✅ 已确认 | ❌ 遗漏 | ⚠️ 额外变更 |
|-----------|---------|--------------|
| 变更说明「为用户等级维度表(dim_user_level)新增 discount_rate 折扣率字段，并初始化各等级折扣值」→ 对应 [01_alter_dim_user_level_add_discount.sql] 中的 ALTER TABLE 和 UPDATE | 无 | [02_create_fact_user_daily_summary.sql] 中执行了 CREATE INDEX idx_fuds_date_id, idx_fuds_user_id, idx_fuds_level_id，变更说明未提及 |
| 变更说明「新建用户每日汇总宽表 fact_user_daily_summary，依赖现有维度表 dim_user_level 和 dim_date」→ 对应 [02_create_fact_user_daily_summary.sql] 中的 CREATE TABLE 和 CREATE INDEX | | |
| 变更说明「新建视图 vw_user_level_revenue，引用脚本①新增的 discount_rate 字段，依赖现有的 fact_orders 和 dim_user_level」→ 对应 [03_create_view_vw_user_level_revenue.sql] 中的 CREATE VIEW | | |

## 高风险操作清单

🔴 **[03_create_view_vw_user_level_revenue.sql]**: 视图定义中可能未使用 `discount_rate` 字段（变更说明声称引用该字段，但实际影响汇总指出“视图定义中可能未使用该字段”）— 功能与说明不一致，需确认视图是否真正使用了 `discount_rate`。

🔴 **[03_create_view_vw_user_level_revenue.sql]**: 视图依赖 `fact_orders` 的 `level_id` 字段，但该字段允许为空 — 可能导致聚合结果不准确或丢失部分数据。

## 数据库变更影响详情

### 新增对象列表

| 对象全名 | 类型 | 所属脚本 |
|----------|------|----------|
| `public.fact_user_daily_summary` | TABLE | 02_create_fact_user_daily_summary.sql |
| `public.idx_fuds_date_id` | INDEX | 02_create_fact_user_daily_summary.sql |
| `public.idx_fuds_user_id` | INDEX | 02_create_fact_user_daily_summary.sql |
| `public.idx_fuds_level_id` | INDEX | 02_create_fact_user_daily_summary.sql |
| `public.vw_user_level_revenue` | VIEW | 03_create_view_vw_user_level_revenue.sql |

### 修改对象列表

| 对象全名 | 类型 | 修改内容 |
|----------|------|----------|
| `public.dim_user_level` | TABLE | 新增 `discount` 字段（类型未知），并通过 UPDATE 语句为历史记录填充该字段值 |

### 删除对象列表

无

### 数据变更影响

| 受影响的表 | 变更说明 |
|------------|----------|
| `public.dim_user_level` | 通过 UPDATE 语句为历史记录填充新增的 `discount` 字段值 |

## 执行顺序与依赖关系说明

建议按以下顺序执行脚本（已确认无循环依赖）：

1. **01_alter_dim_user_level_add_discount.sql** — 先为维度表增加折扣字段，作为后续脚本的基础。
2. **02_create_fact_user_daily_summary.sql** — 创建事实表及索引，该表不依赖其他脚本。
3. **03_create_view_vw_user_level_revenue.sql** — 创建视图，依赖 `dim_user_level` 和 `fact_orders`（注意：视图定义中可能未使用 `discount` 字段，且 `fact_orders.level_id` 允许为空）。

依赖关系分析：脚本间无直接跨文件数据依赖，但视图逻辑上依赖脚本①修改的 `dim_user_level` 表。建议严格按上述顺序执行。

## 审核结论与建议

**⚠️ 需整改后发版**

存在以下必须整改的问题：

1. **确认视图 `vw_user_level_revenue` 是否真正使用了 `discount_rate` 字段**。若未使用，需更新变更说明或修正视图定义，确保文档与实现一致。
2. **评估 `fact_orders.level_id` 允许为空对视图聚合结果的影响**。建议在视图定义中添加 `WHERE level_id IS NOT NULL` 过滤，或确认业务上不会出现空值导致数据丢失。

整改完成后，建议重新审核并确认风险消除，方可发版。