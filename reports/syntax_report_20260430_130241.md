# SQL 脚本语法与质量分析报告

## 质量评分概要

| 指标 | 数值 |
|------|------|
| **质量评分** | **88 / 100** |
| 脚本文件总数 | 3 |
| ERROR 数量 | 1 |
| WARNING 数量 | 0 |
| INFO 数量 | 4 |

## 各文件质量概览

| 文件名 | ERROR | WARNING | INFO | 主要问题 |
|--------|-------|---------|------|----------|
| `01_alter_dim_user_level_add_discount.sql` | 0 | 0 | 4 | UPDATE 目标表在快照中不存在（4次） |
| `02_create_fact_user_daily_summary.sql` | 0 | 0 | 0 | 无问题 |
| `03_create_view_vw_user_level_revenue.sql` | 1 | 0 | 0 | 视图脚本语法错误，缺少 expression 关键字 |

## 🔴 ERROR 级问题明细

### 文件：`03_create_view_vw_user_level_revenue.sql`

| 规则 | 描述 | 涉及 SQL 片段 | 修复建议 |
|------|------|---------------|----------|
| `SYNTAX_ERROR` | Required keyword: 'expression' missing for `<class 'sqlglot.expressions.ddl.Comment'>` | `COMMENT ON VIEW public IS` | 修复语法错误后重新提交 |

## 🟡 WARNING 级问题明细

无 WARNING 级别问题。

## 🔵 INFO 级问题明细

### 文件：`01_alter_dim_user_level_add_discount.sql`（共 4 条，均为同类问题）

| 规则 | 描述 | 涉及 SQL 片段 | 修复建议 |
|------|------|---------------|----------|
| `DML_TARGET_NOT_IN_DB` | UPDATE 的目标对象 'public.dim_user_level' 在当前数据库快照中不存在，可能由本次发版的其他脚本新建 | `/* 初始化各等级折扣值 */ UPDATE public.dim_user_level SET discount_rate = 0.00 WHERE level_name = '普通用户'` | 确认目标表由同批次其他脚本创建，并保证执行顺序正确 |
| `DML_TARGET_NOT_IN_DB` | UPDATE 的目标对象 'public.dim_user_level' 在当前数据库快照中不存在，可能由本次发版的其他脚本新建 | `UPDATE public.dim_user_level SET discount_rate = 0.05 WHERE level_name = '银牌用户'` | 确认目标表由同批次其他脚本创建，并保证执行顺序正确 |
| `DML_TARGET_NOT_IN_DB` | UPDATE 的目标对象 'public.dim_user_level' 在当前数据库快照中不存在，可能由本次发版的其他脚本新建 | `UPDATE public.dim_user_level SET discount_rate = 0.10 WHERE level_name = '金牌用户'` | 确认目标表由同批次其他脚本创建，并保证执行顺序正确 |
| `DML_TARGET_NOT_IN_DB` | UPDATE 的目标对象 'public.dim_user_level' 在当前数据库快照中不存在，可能由本次发版的其他脚本新建 | `UPDATE public.dim_user_level SET discount_rate = 0.15 WHERE level_name = '钻石用户'` | 确认目标表由同批次其他脚本创建，并保证执行顺序正确 |

## 专项分析

### 安全风险专项（无 WHERE 的 DML、TRUNCATE、DROP）

- **未发现**：所有 DML 语句均包含 WHERE 条件，无 TRUNCATE 或 DROP 操作。

### 性能规范专项（SELECT *、缺失索引提示等）

- **未发现**：脚本中未使用 `SELECT *`，索引创建语句（`02_create_fact_user_daily_summary.sql`）已明确指定索引字段。

## 改进建议清单

1. **修复 `03_create_view_vw_user_level_revenue.sql` 中的 Comment 语法错误**，确保视图可正常创建。
2. **确认 `01_alter_dim_user_level_add_discount.sql` 中 `dim_user_level` 表由同批次其他脚本创建**，并在发版计划中明确执行顺序（先建表后执行 UPDATE）。
3. **在发版计划中明确脚本执行顺序**，避免因依赖缺失导致执行失败。
4. **对重复的 INFO 问题统一处理**，减少冗余告警（例如合并为一条提示）。
5. **增加数据库快照验证步骤**，提前发现依赖缺失，降低发版风险。