# SQL 脚本变更说明对比报告

| 项目 | 内容 |
|------|------|
| **生成时间** | 2026-04-30T14:14:52.306077 |
| **文件数量** | 3 |
| **总体判定** | ⚠️ WARN |
| **风险等级** | MEDIUM |

## 执行概要

本次发版涉及 3 个 SQL 脚本，主要变更包括：为维度表 `dim_user_level` 新增折扣字段并回填历史数据、新建事实表 `fact_user_daily_summary` 及其索引、新建视图 `vw_user_level_revenue`。变更说明与脚本实际影响基本一致，但存在以下问题：① 变更说明中字段名为 `discount_rate`，脚本实际新增列名为 `discount`，命名不一致但功能对应；② 脚本额外创建了 3 个索引，变更说明未提及；③ 视图创建存在语法错误（解析报告指出 1 个语法错误），可能导致创建失败。无缺失功能项，无高危破坏性操作，综合判定为 **WARN**，需修复视图语法错误后重新执行。

## 变更覆盖情况

| ✅ 已确认 | ❌ 遗漏 | ⚠️ 额外变更 |
|-----------|---------|--------------|
| 变更说明「为用户等级维度表(dim_user_level)新增 discount_rate 折扣率字段，并初始化各等级折扣值」→ 对应 [01_alter_dim_user_level_add_discount.sql] 中的 ALTER TABLE 新增 discount 列及 UPDATE 语句 | 无 | 02_create_fact_user_daily_summary.sql 中创建了索引 idx_fuds_date_id、idx_fuds_user_id、idx_fuds_level_id，变更说明未提及索引创建 |
| 变更说明「新建用户每日汇总宽表 fact_user_daily_summary，依赖现有维度表 dim_user_level 和 dim_date」→ 对应 [02_create_fact_user_daily_summary.sql] 中的 CREATE TABLE 及索引创建 | | |
| 变更说明「新建视图 vw_user_level_revenue，引用脚本①新增的 discount_rate 字段，依赖现有的 fact_orders 和 dim_user_level」→ 对应 [03_create_view_vw_user_level_revenue.sql] 中的 CREATE VIEW | | |

## 高风险操作清单

- 🔴 **03_create_view_vw_user_level_revenue.sql**: 视图创建存在语法错误（解析报告指出 1 个语法错误）— 可能导致视图创建失败，需修复后重新执行。

## 数据库变更影响详情

### 新增对象列表

| 对象全名 | 类型 | 来源文件 |
|----------|------|----------|
| `public.fact_user_daily_summary` | TABLE | 02_create_fact_user_daily_summary.sql |
| `public.idx_fuds_date_id` | INDEX | 02_create_fact_user_daily_summary.sql |
| `public.idx_fuds_user_id` | INDEX | 02_create_fact_user_daily_summary.sql |
| `public.idx_fuds_level_id` | INDEX | 02_create_fact_user_daily_summary.sql |
| `public.vw_user_level_revenue` | VIEW | 03_create_view_vw_user_level_revenue.sql |

### 修改对象列表

| 对象全名 | 类型 | 修改内容 |
|----------|------|----------|
| `public.dim_user_level` | TABLE | 新增 `discount` 列（类型推测为数值型），并执行 UPDATE 语句填充该列的历史数据 |

### 删除对象列表

无

### 数据变更影响

| 表名 | 变更类型 | 说明 |
|------|----------|------|
| `public.dim_user_level` | 数据更新 | 通过 UPDATE 语句填充新增的 `discount` 列的历史数据 |

## 执行顺序与依赖关系说明

**执行顺序**（由依赖图计算得出，无循环依赖）：

1. `01_alter_dim_user_level_add_discount.sql`
2. `02_create_fact_user_daily_summary.sql`
3. `03_create_view_vw_user_level_revenue.sql`

**依赖关系分析**：
- `01_alter_dim_user_level_add_discount.sql` 无前置依赖，先修改 `dim_user_level` 表结构并更新数据。
- `02_create_fact_user_daily_summary.sql` 创建事实表及索引，不直接依赖 01 的修改，但逻辑上依赖 `dim_user_level` 和 `dim_date` 表存在（均为已有表）。
- `03_create_view_vw_user_level_revenue.sql` 创建的视图依赖 `dim_user_level`（已修改）和 `fact_orders`（已有表），因此放在最后执行合理。

**跨文件依赖**：
- 01 修改的 `dim_user_level.discount` 列可能被 03 的视图引用用于计算折扣后收入。
- 02 创建的事实表可能依赖 `dim_user_level.level_id`，但未直接引用 `discount` 列。
- 03 的视图依赖 `dim_user_level`（已修改）和 `fact_orders`，形成跨文件逻辑依赖。

## 审核结论与建议

**判定：需整改后发版**

- 必须修复 `03_create_view_vw_user_level_revenue.sql` 中的语法错误，确保视图创建成功。
- 建议统一字段命名：变更说明中的 `discount_rate` 与脚本中的 `discount` 存在不一致，建议在文档或脚本中统一，避免后续混淆。
- 额外创建的索引属于合理补充，无需调整，但建议在变更说明中补充记录。
- 修复后重新执行全部脚本，并验证视图创建成功及数据正确性。