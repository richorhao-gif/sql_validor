# SQL 脚本语法与质量分析报告

| 字段 | 值 |
|---|---|
| 生成时间 | 2026-05-05 22:44:51 |
| 分析文件数 | 3 |
| 综合质量评分 | 🔴 **0 / 100** |
| ERROR 数 | 14 |
| WARNING 数 | 0 |
| INFO 数 | 6 |

---

## 各文件质量概览

| 文件名 | ERROR | WARNING | INFO | 主要问题摘要 |
|---|---|---|---|---|
| `01_sauce_mat_brd_prdct_atribt_redeploy.sql` | 6 | 0 | 3 | COMMENT ON VIEW 语法错误（缺少视图名后的点号），CREATE TABLE 缺少 IF NOT EXISTS 幂等性保护 |
| `02_sauce_mat_abbr_add_column.sql` | 4 | 0 | 0 | COMMENT ON VIEW 语法错误（缺少视图名后的点号） |
| `03_sauce_mat_variety_new_asset.sql` | 4 | 0 | 3 | COMMENT ON VIEW 语法错误（缺少视图名后的点号），CREATE TABLE 缺少 IF NOT EXISTS 幂等性保护 |

---

## 🔴 ERROR 级问题（14 项）

### E1 · `01_sauce_mat_brd_prdct_atribt_redeploy.sql` — SYNTAX_ERROR

**规则：** `SYNTAX_ERROR`  
**严重度：** ERROR  
**描述：** COMMENT ON VIEW 语句缺少视图名后的点号（.），导致解析器无法识别视图名称。静态工具已发现此问题，但描述不够精确。

**涉及片段：**
```sql
COMMENT ON VIEW ods IS '调味物料品种与产品属性-ODS视图';
```

**建议：** 修正为：COMMENT ON VIEW ods.v_ods_mannual_sauce_mat_brd_prdct_atribt IS '调味物料品种与产品属性-ODS视图';

### E2 · `01_sauce_mat_brd_prdct_atribt_redeploy.sql` — SYNTAX_ERROR

**规则：** `SYNTAX_ERROR`  
**严重度：** ERROR  
**描述：** COMMENT ON VIEW 语句缺少视图名后的点号（.），导致解析器无法识别视图名称。

**涉及片段：**
```sql
COMMENT ON VIEW dwd IS '调味物料品种与产品属性-DWD视图（含反向增量CTE）';
```

**建议：** 修正为：COMMENT ON VIEW dwd.v_dwd_scd_sauce_mat_brd_prdct_atribt IS '调味物料品种与产品属性-DWD视图（含反向增量CTE）';

### E3 · `01_sauce_mat_brd_prdct_atribt_redeploy.sql` — SYNTAX_ERROR

**规则：** `SYNTAX_ERROR`  
**严重度：** ERROR  
**描述：** COMMENT ON VIEW 语句缺少视图名后的点号（.），导致解析器无法识别视图名称。

**涉及片段：**
```sql
COMMENT ON VIEW da IS '调味物料品种与产品属性-DA视图（含反向增量CTE）';
```

**建议：** 修正为：COMMENT ON VIEW da.v_da_td_scd_sauce_mat_brd_prdct_atribt IS '调味物料品种与产品属性-DA视图（含反向增量CTE）';

### E4 · `01_sauce_mat_brd_prdct_atribt_redeploy.sql` — SYNTAX_ERROR

**规则：** `SYNTAX_ERROR`  
**严重度：** ERROR  
**描述：** COMMENT ON VIEW 语句缺少视图名后的点号（.），导致解析器无法识别视图名称。

**涉及片段：**
```sql
COMMENT ON VIEW da IS '调味物料完整宽视图（品种+简称，含产品属性）';
```

**建议：** 修正为：COMMENT ON VIEW da.v_da_sauce_material_full IS '调味物料完整宽视图（品种+简称，含产品属性）';

### E5 · `01_sauce_mat_brd_prdct_atribt_redeploy.sql` — SYNTAX_ERROR

**规则：** `SYNTAX_ERROR`  
**严重度：** ERROR  
**描述：** COMMENT ON VIEW 语句缺少视图名后的点号（.），导致解析器无法识别视图名称。

**涉及片段：**
```sql
COMMENT ON VIEW da_selfhp IS '调味物料品种与产品属性-BI中文查询视图（security_invoker）';
```

**建议：** 修正为：COMMENT ON VIEW da_selfhp.da_td_scd_sauce_mat_brd_prdct_atribt_调味物料品种与产品属性 IS '调味物料品种与产品属性-BI中文查询视图（security_invoker）';

### E6 · `01_sauce_mat_brd_prdct_atribt_redeploy.sql` — SYNTAX_ERROR

**规则：** `SYNTAX_ERROR`  
**严重度：** ERROR  
**描述：** COMMENT ON VIEW 语句缺少视图名后的点号（.），导致解析器无法识别视图名称。

**涉及片段：**
```sql
COMMENT ON VIEW da_selfhp IS 'BI全量物料视图（含产品属性和简称，security_invoker）';
```

**建议：** 修正为：COMMENT ON VIEW da_selfhp.v_sauce_material_带简称 IS 'BI全量物料视图（含产品属性和简称，security_invoker）';

### E7 · `02_sauce_mat_abbr_add_column.sql` — SYNTAX_ERROR

**规则：** `SYNTAX_ERROR`  
**严重度：** ERROR  
**描述：** COMMENT ON VIEW 语句缺少视图名后的点号（.），导致解析器无法识别视图名称。

**涉及片段：**
```sql
COMMENT ON VIEW ods IS '调味物料简称-ODS视图（含短简称）';
```

**建议：** 修正为：COMMENT ON VIEW ods.v_ods_mannual_sauce_mat_abbr IS '调味物料简称-ODS视图（含短简称）';

### E8 · `02_sauce_mat_abbr_add_column.sql` — SYNTAX_ERROR

**规则：** `SYNTAX_ERROR`  
**严重度：** ERROR  
**描述：** COMMENT ON VIEW 语句缺少视图名后的点号（.），导致解析器无法识别视图名称。

**涉及片段：**
```sql
COMMENT ON VIEW dwd IS '调味物料简称-DWD视图（含短简称）';
```

**建议：** 修正为：COMMENT ON VIEW dwd.v_dwd_scd_sauce_mat_abbr IS '调味物料简称-DWD视图（含短简称）';

### E9 · `02_sauce_mat_abbr_add_column.sql` — SYNTAX_ERROR

**规则：** `SYNTAX_ERROR`  
**严重度：** ERROR  
**描述：** COMMENT ON VIEW 语句缺少视图名后的点号（.），导致解析器无法识别视图名称。

**涉及片段：**
```sql
COMMENT ON VIEW da IS '调味物料完整宽视图（品种+简称+短简称）';
```

**建议：** 修正为：COMMENT ON VIEW da.v_da_sauce_material_full IS '调味物料完整宽视图（品种+简称+短简称）';

### E10 · `02_sauce_mat_abbr_add_column.sql` — SYNTAX_ERROR

**规则：** `SYNTAX_ERROR`  
**严重度：** ERROR  
**描述：** COMMENT ON VIEW 语句缺少视图名后的点号（.），导致解析器无法识别视图名称。

**涉及片段：**
```sql
COMMENT ON VIEW da_selfhp IS 'BI全量物料视图（含短简称，security_invoker）';
```

**建议：** 修正为：COMMENT ON VIEW da_selfhp.v_sauce_material_带简称 IS 'BI全量物料视图（含短简称，security_invoker）';

### E11 · `03_sauce_mat_variety_new_asset.sql` — SYNTAX_ERROR

**规则：** `SYNTAX_ERROR`  
**严重度：** ERROR  
**描述：** COMMENT ON VIEW 语句缺少视图名后的点号（.），导致解析器无法识别视图名称。

**涉及片段：**
```sql
COMMENT ON VIEW ods IS '调味品品种-ODS视图';
```

**建议：** 修正为：COMMENT ON VIEW ods.v_ods_mannual_sauce_mat_variety IS '调味品品种-ODS视图';

### E12 · `03_sauce_mat_variety_new_asset.sql` — SYNTAX_ERROR

**规则：** `SYNTAX_ERROR`  
**严重度：** ERROR  
**描述：** COMMENT ON VIEW 语句缺少视图名后的点号（.），导致解析器无法识别视图名称。

**涉及片段：**
```sql
COMMENT ON VIEW dwd IS '调味品品种-DWD视图（含反向增量CTE）';
```

**建议：** 修正为：COMMENT ON VIEW dwd.v_dwd_scd_sauce_mat_variety IS '调味品品种-DWD视图（含反向增量CTE）';

### E13 · `03_sauce_mat_variety_new_asset.sql` — SYNTAX_ERROR

**规则：** `SYNTAX_ERROR`  
**严重度：** ERROR  
**描述：** COMMENT ON VIEW 语句缺少视图名后的点号（.），导致解析器无法识别视图名称。

**涉及片段：**
```sql
COMMENT ON VIEW da IS '调味品品种-DA视图（含反向增量CTE）';
```

**建议：** 修正为：COMMENT ON VIEW da.v_da_td_scd_sauce_mat_variety IS '调味品品种-DA视图（含反向增量CTE）';

### E14 · `03_sauce_mat_variety_new_asset.sql` — SYNTAX_ERROR

**规则：** `SYNTAX_ERROR`  
**严重度：** ERROR  
**描述：** COMMENT ON VIEW 语句缺少视图名后的点号（.），导致解析器无法识别视图名称。

**涉及片段：**
```sql
COMMENT ON VIEW da_selfhp IS '调味品品种-BI中文查询视图（security_invoker，全新资产）';
```

**建议：** 修正为：COMMENT ON VIEW da_selfhp.sauce_mat_variety_调味品品种 IS '调味品品种-BI中文查询视图（security_invoker，全新资产）';


---

## 🟡 WARNING 级问题（0 项）

本次发版无 WARNING 级问题。

---

## 🔵 INFO 级问题（6 项）

| # | 文件 | 规则 | 描述 |
|---|---|---|---|
| 1 | `01_sauce_mat_brd_prdct_atribt_redeploy.sql` | `CREATE_NO_IF_NOT_EXISTS` | CREATE TABLE 语句未使用 IF NOT EXISTS，若表已存在（例如因前序脚本执行中断导致残留），脚本会报错中断。 |
| 2 | `01_sauce_mat_brd_prdct_atribt_redeploy.sql` | `CREATE_NO_IF_NOT_EXISTS` | CREATE TABLE 语句未使用 IF NOT EXISTS，若表已存在会报错。 |
| 3 | `01_sauce_mat_brd_prdct_atribt_redeploy.sql` | `CREATE_NO_IF_NOT_EXISTS` | CREATE TABLE 语句未使用 IF NOT EXISTS，若表已存在会报错。 |
| 4 | `03_sauce_mat_variety_new_asset.sql` | `CREATE_NO_IF_NOT_EXISTS` | CREATE TABLE 语句未使用 IF NOT EXISTS，若表已存在会报错。 |
| 5 | `03_sauce_mat_variety_new_asset.sql` | `CREATE_NO_IF_NOT_EXISTS` | CREATE TABLE 语句未使用 IF NOT EXISTS，若表已存在会报错。 |
| 6 | `03_sauce_mat_variety_new_asset.sql` | `CREATE_NO_IF_NOT_EXISTS` | CREATE TABLE 语句未使用 IF NOT EXISTS，若表已存在会报错。 |

---

## 专项分析

### 🛡️ 安全风险

| 风险类型 | 数量 | 详情 |
|---|---|---|
| 无 WHERE 的 DELETE | 0 | — |
| 无 WHERE 的 UPDATE | 0 | — |
| TRUNCATE 操作 | 0 | — |
| DROP 操作 | 0 | — |

### ⚡ 性能规范

| 问题类型 | 数量 | 详情 |
|---|---|---|
| SELECT * | 0 | — |

---

## 改进建议

1. 1. 修复所有 COMMENT ON VIEW 语法错误：在视图名前加上 schema 名和点号（如 ods.v_xxx），这是最高优先级的阻塞性问题。
2. 2. 为所有 CREATE TABLE 语句添加 IF NOT EXISTS 子句，提升脚本幂等性，避免因残留表导致部署中断。
3. 3. 考虑为 DWD 和 DA 层视图中的 delta_records CTE 添加索引建议（如 (plant_code, mat_code)），以优化增量查询性能。
4. 4. 建议在文件 01 和 02 的宽视图重建前，确认 da.da_td_scd_sauce_mat_abbr 表已存在 mat_abbr_short 列，避免因执行顺序问题导致视图创建失败。

---

*本报告由 SQL 变更校验智能体自动生成*