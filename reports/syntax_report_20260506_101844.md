# SQL 脚本语法与质量分析报告

| 字段 | 值 |
|---|---|
| 生成时间 | 2026-05-06 10:18:44 |
| 分析文件数 | 3 |
| 综合质量评分 | 🔴 **55 / 100** |
| ERROR 数 | 5 |
| WARNING 数 | 2 |
| INFO 数 | 5 |

---

## 各文件质量概览

| 文件名 | ERROR | WARNING | INFO | 主要问题摘要 |
|---|---|---|---|---|
| `01_sauce_mat_brd_prdct_atribt_redeploy.sql` | 6 | 3 | 3 | COMMENT ON VIEW 语法错误（6处）；恒真条件导致逻辑失效（2处）；CREATE TABLE 缺少 IF NOT EXISTS（3处）；ODS 表缺少 COMMENT ON 部分列；DWD/DA 视图 CTE 逻辑冗余 |
| `02_sauce_mat_abbr_add_column.sql` | 4 | 0 | 1 | COMMENT ON VIEW 语法错误（4处）；ODS 视图重建后缺少 COMMENT ON 部分列 |
| `03_sauce_mat_variety_new_asset.sql` | 4 | 2 | 3 | COMMENT ON VIEW 语法错误（4处）；恒真条件导致逻辑失效（2处）；CREATE TABLE 缺少 IF NOT EXISTS（3处）；ODS 表缺少 COMMENT ON 部分列 |

---

## 🔴 ERROR 级问题（5 项）

### E1 · `01_sauce_mat_brd_prdct_atribt_redeploy.sql` — SYNTAX_ERROR

**规则：** `SYNTAX_ERROR`  
**严重度：** ERROR  
**描述：** COMMENT ON VIEW 语句缺少视图名称后的 IS 关键字和注释内容。静态工具已发现此问题，但描述不够精确。实际代码中，COMMENT ON VIEW 后面直接跟了 schema 名（如 'ods'），缺少完整的视图对象名（如 'ods.v_ods_mannual_sauce_mat_brd_prdct_atribt'）和 IS 关键字。共6处：ods、dwd、da（2处）、da_selfhp（2处）。

**涉及片段：**
```sql
COMMENT ON VIEW ods IS
    '调味物料品种与产品属性-ODS视图';
```

**建议：** 修正为：COMMENT ON VIEW ods.v_ods_mannual_sauce_mat_brd_prdct_atribt IS '调味物料品种与产品属性-ODS视图';

### E2 · `01_sauce_mat_brd_prdct_atribt_redeploy.sql` — LOGIC_ERROR

**规则：** `LOGIC_ERROR`  
**严重度：** ERROR  
**描述：** DWD 和 DA 视图的 WHERE 子句包含恒真条件 'TRUE'::text = 'TRUE'::text，导致 OR 后面的 EXISTS 子查询永远无法执行。这是一个严重的逻辑错误，静态工具未发现。

**涉及片段：**
```sql
WHERE 'TRUE'::text = 'TRUE'::text
   OR EXISTS (
        SELECT 1 FROM delta_records dr
        WHERE dr.plant_code = ods.plant_code AND dr.mat_code = ods.mat_code
   );
```

**建议：** 删除恒真条件，直接使用 EXISTS 逻辑：WHERE EXISTS (SELECT 1 FROM delta_records dr WHERE dr.plant_code = ods.plant_code AND dr.mat_code = ods.mat_code); 或根据业务需求调整 WHERE 条件。

### E3 · `02_sauce_mat_abbr_add_column.sql` — SYNTAX_ERROR

**规则：** `SYNTAX_ERROR`  
**严重度：** ERROR  
**描述：** COMMENT ON VIEW 语句缺少视图名称后的 IS 关键字和注释内容。共4处：ods、dwd、da、da_selfhp。静态工具已发现此问题。

**涉及片段：**
```sql
COMMENT ON VIEW ods IS '调味物料简称-ODS视图（含短简称）';
```

**建议：** 修正为：COMMENT ON VIEW ods.v_ods_mannual_sauce_mat_abbr IS '调味物料简称-ODS视图（含短简称）';

### E4 · `03_sauce_mat_variety_new_asset.sql` — SYNTAX_ERROR

**规则：** `SYNTAX_ERROR`  
**严重度：** ERROR  
**描述：** COMMENT ON VIEW 语句缺少视图名称后的 IS 关键字和注释内容。共4处：ods、dwd、da、da_selfhp。静态工具已发现此问题。

**涉及片段：**
```sql
COMMENT ON VIEW ods IS '调味品品种-ODS视图';
```

**建议：** 修正为：COMMENT ON VIEW ods.v_ods_mannual_sauce_mat_variety IS '调味品品种-ODS视图';

### E5 · `03_sauce_mat_variety_new_asset.sql` — LOGIC_ERROR

**规则：** `LOGIC_ERROR`  
**严重度：** ERROR  
**描述：** DWD 和 DA 视图的 WHERE 子句包含恒真条件 'TRUE'::text = 'TRUE'::text，导致 OR 后面的 EXISTS 子查询永远无法执行。与文件1相同的问题。静态工具未发现。

**涉及片段：**
```sql
WHERE 'TRUE'::text = 'TRUE'::text
   OR EXISTS (
        SELECT 1 FROM delta_records dr WHERE dr.variety_code = ods.variety_code
   );
```

**建议：** 删除恒真条件，直接使用 EXISTS 逻辑。


---

## 🟡 WARNING 级问题（2 项）

### W1 · `01_sauce_mat_brd_prdct_atribt_redeploy.sql` — CREATE_NO_IF_NOT_EXISTS

**规则：** `CREATE_NO_IF_NOT_EXISTS`  
**严重度：** WARNING  
**描述：** ODS、DWD、DA 三层的 CREATE TABLE 语句未使用 IF NOT EXISTS。虽然前面有 DROP TABLE IF EXISTS，但若脚本因异常中断后重跑，DROP 可能已执行但 CREATE 未执行，再次运行会因表已存在而报错。静态工具已发现此问题。

**涉及片段：**
```sql
CREATE TABLE ods.ods_mannual_sauce_mat_brd_prdct_atribt (
    plant_code       varchar,
    ...
```

**建议：** 改为 CREATE TABLE IF NOT EXISTS 以提升幂等性。

### W2 · `03_sauce_mat_variety_new_asset.sql` — CREATE_NO_IF_NOT_EXISTS

**规则：** `CREATE_NO_IF_NOT_EXISTS`  
**严重度：** WARNING  
**描述：** ODS、DWD、DA 三层的 CREATE TABLE 语句未使用 IF NOT EXISTS。静态工具已发现此问题。

**涉及片段：**
```sql
CREATE TABLE ods.ods_mannual_sauce_mat_variety (
    variety_code  varchar,
    ...
```

**建议：** 改为 CREATE TABLE IF NOT EXISTS 以提升幂等性。


---

## 🔵 INFO 级问题（5 项）

| # | 文件 | 规则 | 描述 |
|---|---|---|---|
| 1 | `01_sauce_mat_brd_prdct_atribt_redeploy.sql` | `NO_COMMENT` | ODS 表 ods.ods_mannual_sauce_mat_brd_prdct_atribt 的 COMMENT ON 只覆盖了新增列 is_rawmat、… |
| 2 | `01_sauce_mat_brd_prdct_atribt_redeploy.sql` | `LOGIC_ERROR` | DWD 和 DA 视图中的 CTE (delta_records) 逻辑冗余。子查询 SELECT delta.plant_code, delta.mat_co… |
| 3 | `02_sauce_mat_abbr_add_column.sql` | `NO_COMMENT` | ODS 视图 ods.v_ods_mannual_sauce_mat_abbr 重建后，COMMENT ON VIEW 已添加，但缺少对新增列 mat_abbr… |
| 4 | `03_sauce_mat_variety_new_asset.sql` | `NO_COMMENT` | ODS 表 ods.ods_mannual_sauce_mat_variety 的 COMMENT ON 只覆盖了 variety_code、parent_co… |
| 5 | `03_sauce_mat_variety_new_asset.sql` | `LOGIC_ERROR` | DWD 和 DA 视图中的 CTE (delta_records) 逻辑冗余，与文件1相同的问题。count(1) 未被使用。静态工具未发现此问题。 |

---

## 专项分析

### 🛡️ 安全风险

| 风险类型 | 数量 | 详情 |
|---|---|---|
| 无 WHERE 的 DELETE | 0 | — |
| 无 WHERE 的 UPDATE | 0 | — |
| TRUNCATE 操作 | 0 | — |
| DROP 操作 | 0 | — |

### ⚡ 性能规范

| 问题类型 | 数量 | 详情 |
|---|---|---|
| SELECT * | 0 | — |

---

## 改进建议

1. 【严重】修复所有 COMMENT ON VIEW 语法错误：共14处，均缺少完整的视图对象名和 IS 关键字。这是静态工具已发现但描述不够精确的问题，必须修复。
2. 【严重】删除所有恒真条件：文件1和文件3中 DWD/DA 视图的 WHERE 'TRUE'::text = 'TRUE'::text OR EXISTS(...) 导致 EXISTS 逻辑永远不执行。这是静态工具未发现的严重逻辑错误，建议直接使用 EXISTS 子查询。
3. 【建议】简化冗余 CTE 逻辑：文件1和文件3中 DWD/DA 视图的 delta_records CTE 中 count(1) 未被使用，建议改为 SELECT DISTINCT 或直接内联 EXISTS 子查询，提升可读性和性能。
4. 【建议】补充缺失的列注释：文件1的 ODS 表缺少原有列的 COMMENT ON；文件3的 ODS 表缺少部分列的 COMMENT ON。建议补全所有列的注释，提升可维护性。
5. 【建议】考虑添加 IF NOT EXISTS：虽然当前脚本有 DROP 前置操作，但为提升幂等性，建议在 CREATE TABLE 语句中添加 IF NOT EXISTS。

---

*本报告由 SQL 变更校验智能体自动生成*