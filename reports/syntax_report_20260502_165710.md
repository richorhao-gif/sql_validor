# SQL 脚本语法与质量分析报告

## 质量评分概要

| 指标 | 数值 |
|------|------|
| **质量评分** | **88 / 100** |
| 检查文件数 | 3 |
| 🔴 ERROR | 1 |
| 🟡 WARNING | 0 |
| 🔵 INFO | 4 |

---

## 各文件质量概览

| 文件名 | ERROR | WARNING | INFO | 主要问题 |
|--------|-------|---------|------|----------|
| `01_alter_dim_user_level_add_discount.sql` | 0 | 0 | 4 | 目标表 `dim_user_level` 在当前快照中不存在（可能由同批次其他脚本创建） |
| `02_create_fact_user_daily_summary.sql` | 0 | 0 | 0 | 无问题 |
| `03_create_view_vw_user_level_revenue.sql` | 1 | 0 | 0 | 语法错误：缺少 `expression` 关键字 |

---

## 🔴 ERROR 级问题明细

### 文件：`03_create_view_vw_user_level_revenue.sql`

| 规则 | 描述 | 涉及 SQL 片段 | 修复建议 |
|------|------|---------------|----------|
| `SYNTAX_ERROR` | Required keyword: 'expression' missing for `<class 'sqlglot.expressions.ddl.Comment'>` | `COMMENT ON VIEW public IS` | 修复语法错误后重新提交 |

---

## 🟡 WARNING 级问题明细

无 WARNING 级别问题。

---

## 🔵 INFO 级问题明细

### 文件：`01_alter_dim_user_level_add_discount.sql`（共 4 条，规则相同）

| 规则 | 描述 | 涉及 SQL 片段 | 修复建议 |
|------|------|---------------|----------|
| `DML_TARGET_NOT_IN_DB` | UPDATE 的目标对象 `public.dim_user_level` 在当前数据库快照中不存在，可能由本次发版的其他脚本新建 | `UPDATE public.dim_user_level SET discount_rate = 0.00 WHERE level_name = '普通用户'` | 确认目标表由同批次其他脚本创建，并保证执行顺序正确 |
| `DML_TARGET_NOT_IN_DB` | 同上 | `UPDATE public.dim_user_level SET discount_rate = 0.05 WHERE level_name = '银牌用户'` | 同上 |
| `DML_TARGET_NOT_IN_DB` | 同上 | `UPDATE public.dim_user_level SET discount_rate = 0.10 WHERE level_name = '金牌用户'` | 同上 |
| `DML_TARGET_NOT_IN_DB` | 同上 | `UPDATE public.dim_user_level SET discount_rate = 0.15 WHERE level_name = '钻石用户'` | 同上 |

---

## 专项分析

### 安全风险专项（无 WHERE 的 DML、TRUNCATE、DROP）

- **未发现**：所有 DML 语句均包含 `WHERE` 条件，无 `TRUNCATE` 或 `DROP` 操作。

### 性能规范专项（SELECT *、缺失索引提示等）

- **未发现**：脚本中未使用 `SELECT *`，索引创建语句（`02_create_fact_user_daily_summary.sql`）已明确指定索引字段。

---

## 改进建议清单

1. **修复语法错误**：修正 `03_create_view_vw_user_level_revenue.sql` 中的 `COMMENT ON VIEW` 语句，补充缺失的 `expression` 关键字。
2. **确保执行顺序**：将 `01_alter_dim_user_level_add_discount.sql` 安排在创建 `dim_user_level` 表的脚本之后执行，避免 DML 目标表不存在。
3. **明确依赖关系**：在发版说明中显式声明脚本之间的执行顺序依赖（如 `01_alter` 依赖表创建脚本）。
4. **使用事务或版本控制**：对 DDL 与 DML 混合的批次，建议使用事务包裹或版本控制工具（如 Flyway、Liquibase）保证顺序正确。
5. **增加 CI 检查**：在持续集成流程中加入跨脚本依赖检测，提前发现类似 `DML_TARGET_NOT_IN_DB` 的问题。