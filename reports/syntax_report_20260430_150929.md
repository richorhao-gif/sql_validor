# SQL 脚本语法与质量分析报告

## 质量评分概要

| 指标 | 数值 |
|------|------|
| **质量评分** | **88 / 100** |
| 检查文件数 | 3 |
| 🔴 ERROR 数 | 1 |
| 🟡 WARNING 数 | 0 |
| 🔵 INFO 数 | 4 |

## 各文件质量概览

| 文件名 | 🔴 ERROR | 🟡 WARNING | 🔵 INFO | 主要问题 |
|--------|----------|------------|---------|----------|
| `01_alter_dim_user_level_add_discount.sql` | 0 | 0 | 4 | UPDATE 目标表 `dim_user_level` 在当前快照中不存在（4次重复） |
| `02_create_fact_user_daily_summary.sql` | 0 | 0 | 0 | 无问题 |
| `03_create_view_vw_user_level_revenue.sql` | 1 | 0 | 0 | COMMENT ON VIEW 语法错误，缺少 expression 关键字 |

## 🔴 ERROR 级问题明细

### 文件：`03_create_view_vw_user_level_revenue.sql`

| 字段 | 内容 |
|------|------|
| **规则名** | `SYNTAX_ERROR` |
| **严重级别** | 🔴 ERROR |
| **问题描述** | Required keyword: 'expression' missing for `<class 'sqlglot.expressions.ddl.Comment'>` |
| **涉及 SQL 片段** | ```sql
COMMENT ON VIEW public IS
``` |
| **修复建议** | 修复语法错误后重新提交。检查 COMMENT ON VIEW 语句的完整语法，例如：`COMMENT ON VIEW public.vw_user_level_revenue IS '描述内容';` |

## 🟡 WARNING 级问题明细

无 WARNING 级别问题。

## 🔵 INFO 级问题明细

### 文件：`01_alter_dim_user_level_add_discount.sql`（共 4 条，内容相同，仅 SQL 片段不同）

| # | 规则名 | 问题描述 | 涉及 SQL 片段 | 修复建议 |
|---|--------|----------|---------------|----------|
| 1 | `DML_TARGET_NOT_IN_DB` | UPDATE 的目标对象 `public.dim_user_level` 在当前数据库快照中不存在，可能由本次发版的其他脚本新建 | ```sql
/* 初始化各等级折扣值 */ UPDATE public.dim_user_level SET discount_rate = 0.00 WHERE level_name = '普通用户'
``` | 确认目标表由同批次其他脚本创建，并保证执行顺序正确 |
| 2 | `DML_TARGET_NOT_IN_DB` | 同上 | ```sql
UPDATE public.dim_user_level SET discount_rate = 0.05 WHERE level_name = '银牌用户'
``` | 同上 |
| 3 | `DML_TARGET_NOT_IN_DB` | 同上 | ```sql
UPDATE public.dim_user_level SET discount_rate = 0.10 WHERE level_name = '金牌用户'
``` | 同上 |
| 4 | `DML_TARGET_NOT_IN_DB` | 同上 | ```sql
UPDATE public.dim_user_level SET discount_rate = 0.15 WHERE level_name = '钻石用户'
``` | 同上 |

> 注：以上 4 条 INFO 问题本质相同，建议合并处理，避免重复提示。

## 专项分析

### 安全风险专项（无 WHERE 的 DML、TRUNCATE、DROP）

- 本次检查的所有 DML 语句（UPDATE）均包含 `WHERE` 条件，未发现无限制的更新或删除操作。
- 未发现 `TRUNCATE` 或 `DROP` 等高危操作。
- **结论：无安全风险。**

### 性能规范专项（SELECT *、缺失索引提示等）

- 未发现 `SELECT *` 等性能不良写法。
- 文件 `02_create_fact_user_daily_summary.sql` 中创建了三个索引（日期、用户、等级），符合性能优化规范。
- **结论：性能规范良好。**

## 改进建议清单

1. **修复 `03_create_view_vw_user_level_revenue.sql` 中的 COMMENT 语法错误**：补充完整的 `COMMENT ON VIEW schema.view_name IS '描述';` 语句。
2. **确保 `dim_user_level` 表在 UPDATE 之前创建**：检查发版脚本的执行顺序，将建表/加字段脚本（如 `01_alter_dim_user_level_add_discount.sql` 中的 ALTER 操作）置于 UPDATE 之前。
3. **在发版文档中明确脚本执行顺序**：建议按依赖关系排序：先执行 DDL（建表/加字段），再执行 DML（数据更新），最后执行视图创建。
4. **使用依赖检查工具验证表存在性**：在 CI/CD 流程中增加静态分析或快照对比，提前发现目标对象缺失问题。
5. **减少重复的 INFO 提示，合并同类问题**：对于同一文件、同一规则、同一目标表的多次重复提示，建议聚合为一条，并列出所有受影响的 SQL 行号或片段。