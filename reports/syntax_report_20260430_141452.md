# SQL 脚本语法与质量分析报告

## 质量评分概要

| 指标 | 数值 |
|------|------|
| **质量评分** | **88 / 100** |
| 脚本文件总数 | 3 |
| 🔴 ERROR 数 | 1 |
| 🟡 WARNING 数 | 0 |
| 🔵 INFO 数 | 4 |

## 各文件质量概览

| 文件名 | ERROR | WARNING | INFO | 主要问题 |
|--------|-------|---------|------|----------|
| `01_alter_dim_user_level_add_discount.sql` | 0 | 0 | 4 | UPDATE 目标表 `dim_user_level` 在当前快照中不存在（4次重复） |
| `02_create_fact_user_daily_summary.sql` | 0 | 0 | 0 | 无问题 |
| `03_create_view_vw_user_level_revenue.sql` | 1 | 0 | 0 | 视图脚本语法错误，缺少 `expression` 关键字 |

## 🔴 ERROR 级问题明细

### 文件：`03_create_view_vw_user_level_revenue.sql`

| 规则 | 描述 | 涉及 SQL 片段 | 修复建议 |
|------|------|---------------|----------|
| `SYNTAX_ERROR` | Required keyword: 'expression' missing for `<class 'sqlglot.expressions.ddl.Comment'>` | ```sql
COMMENT ON VIEW public IS
``` | 修复语法错误后重新提交，确保 `COMMENT ON VIEW` 语句包含完整的表达式（例如 `COMMENT ON VIEW public.vw_user_level_revenue IS '描述'`） |

## 🟡 WARNING 级问题明细

无 WARNING 级别问题。

## 🔵 INFO 级问题明细

### 文件：`01_alter_dim_user_level_add_discount.sql`（共 4 条，内容相同）

| 规则 | 描述 | 涉及 SQL 片段 | 修复建议 |
|------|------|---------------|----------|
| `DML_TARGET_NOT_IN_DB` | UPDATE 的目标对象 `public.dim_user_level` 在当前数据库快照中不存在，可能由本次发版的其他脚本新建 | ```sql
/* 初始化各等级折扣值 */ UPDATE public.dim_user_level SET discount_rate = 0.00 WHERE level_name = '普通用户'
``` | 确认目标表由同批次其他脚本创建，并保证执行顺序正确 |
| `DML_TARGET_NOT_IN_DB` | 同上（银牌用户） | ```sql
UPDATE public.dim_user_level SET discount_rate = 0.05 WHERE level_name = '银牌用户'
``` | 同上 |
| `DML_TARGET_NOT_IN_DB` | 同上（金牌用户） | ```sql
UPDATE public.dim_user_level SET discount_rate = 0.10 WHERE level_name = '金牌用户'
``` | 同上 |
| `DML_TARGET_NOT_IN_DB` | 同上（钻石用户） | ```sql
UPDATE public.dim_user_level SET discount_rate = 0.15 WHERE level_name = '钻石用户'
``` | 同上 |

## 专项分析

### 安全风险专项

- **无 WHERE 的 DML**：未发现无 WHERE 条件的 UPDATE/DELETE 语句。所有 UPDATE 均带有 `WHERE level_name = ...` 条件，安全风险较低。
- **TRUNCATE / DROP**：未发现 TRUNCATE 或 DROP 操作，无数据丢失风险。

### 性能规范专项

- **SELECT \***：未发现 `SELECT *` 语句，符合规范。
- **缺失索引提示**：`02_create_fact_user_daily_summary.sql` 已明确创建索引，无缺失索引提示问题。
- **其他**：INFO 级别问题中 UPDATE 目标表不存在，若执行顺序不当可能导致运行时错误，建议在发版前确认表创建脚本的执行顺序。

## 改进建议清单

1. **修复 `03_create_view_vw_user_level_revenue.sql` 中的语法错误**：确保 `COMMENT ON VIEW` 语句包含完整的表达式（如视图名称和注释内容），并重新提交脚本。
2. **确认 `01_alter_dim_user_level_add_discount.sql` 中 UPDATE 的目标表来源**：检查 `dim_user_level` 是否由同批次其他脚本（如 `02_create_fact_user_daily_summary.sql` 或其他未列出的脚本）创建，并在发版说明中明确执行顺序，确保 `01_alter` 脚本在表创建之后执行。
3. **在发版说明中显式声明脚本执行顺序**：例如：`02_create_fact_user_daily_summary.sql` → `01_alter_dim_user_level_add_discount.sql` → `03_create_view_vw_user_level_revenue.sql`，避免依赖缺失。
4. **对 INFO 级别问题也进行确认**：虽然 INFO 级别不影响质量评分，但重复出现 4 次，建议逐一确认目标表是否存在，防止遗漏导致运行时错误。
5. **增加数据库快照一致性检查**：在 CI/CD 流程中增加对当前数据库快照的扫描，提前发现缺失对象（如表、视图），减少发版风险。