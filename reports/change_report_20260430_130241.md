# SQL 脚本变更说明对比报告

| 项目 | 内容 |
|------|------|
| **生成时间** | 2026-04-30T13:02:41.706161 |
| **脚本文件数** | 3 |
| **总体判定** | ⚠️ WARN |
| **风险等级** | MEDIUM |

## 执行概要

本次发版涉及 3 个 SQL 脚本，对数据库进行以下变更：① 为用户等级维度表 `dim_user_level` 新增 `discount` 字段（变更说明中字段名为 `discount_rate`，实际脚本命名为 `discount`，属命名不一致但功能对应），并执行全表 UPDATE 初始化各等级折扣值；② 新建用户每日汇总宽表 `fact_user_daily_summary` 及其三个索引；③ 新建视图 `vw_user_level_revenue`。审核结论为 **WARN**，主要问题包括：变更说明未提及事实表上创建的三个索引（额外变更），以及字段命名不一致。全表 UPDATE 虽为高风险操作，但属于变更说明中已明确的预期行为，不构成未说明风险。整体无破坏性操作，执行顺序合理，建议在确认字段命名统一后发版。

## 变更覆盖情况

| ✅ 已确认 | ❌ 遗漏 | ⚠️ 额外变更 |
|-----------|---------|-------------|
| 变更说明「为用户等级维度表(dim_user_level)新增 discount_rate 折扣率字段，并初始化各等级折扣值」→ 对应 [01_alter_dim_user_level_add_discount.sql] 中的 ALTER TABLE 和 UPDATE | 无 | [02_create_fact_user_daily_summary.sql] 中执行了 CREATE INDEX idx_fuds_date_id, idx_fuds_user_id, idx_fuds_level_id，变更说明未提及 |
| 变更说明「新建用户每日汇总宽表 fact_user_daily_summary，依赖现有维度表 dim_user_level 和 dim_date」→ 对应 [02_create_fact_user_daily_summary.sql] 中的 CREATE TABLE 和 CREATE INDEX | | |
| 变更说明「新建视图 vw_user_level_revenue，引用脚本①新增的 discount_rate 字段，依赖现有的 fact_orders 和 dim_user_level」→ 对应 [03_create_view_vw_user_level_revenue.sql] 中的 CREATE VIEW | | |

## 高风险操作清单

- 🔴 **[01_alter_dim_user_level_add_discount.sql]：全表 UPDATE 填充 discount 字段** — 无 WHERE 条件的全表 UPDATE，属于高风险操作（影响全表行），但变更说明已明确提及初始化各等级折扣值，属于预期行为。

## 数据库变更影响详情

### 新增对象列表

| 对象全名 | 对象类型 | 所属脚本 |
|----------|----------|----------|
| `public.fact_user_daily_summary` | TABLE | 02_create_fact_user_daily_summary.sql |
| `public.idx_fuds_date_id` | INDEX | 02_create_fact_user_daily_summary.sql |
| `public.idx_fuds_user_id` | INDEX | 02_create_fact_user_daily_summary.sql |
| `public.idx_fuds_level_id` | INDEX | 02_create_fact_user_daily_summary.sql |
| `public.vw_user_level_revenue` | VIEW | 03_create_view_vw_user_level_revenue.sql |

### 修改对象列表

| 对象全名 | 对象类型 | 修改内容 |
|----------|----------|----------|
| `public.dim_user_level` | TABLE | 新增 `discount` 字段（类型推测为数值型），并执行 UPDATE 语句填充该字段的历史数据 |

### 删除对象列表

无

### 数据变更影响

| 表名 | 数据变动说明 |
|------|-------------|
| `public.dim_user_level` | 通过 UPDATE 语句填充 `discount` 字段的历史数据，影响全表行 |
| `public.fact_user_daily_summary` | 新表，初始无数据，后续需通过 ETL 加载 |

## 执行顺序与依赖关系说明

**执行顺序（推荐）：**
1. `01_alter_dim_user_level_add_discount.sql`
2. `02_create_fact_user_daily_summary.sql`
3. `03_create_view_vw_user_level_revenue.sql`

**依赖关系分析：**
- `01_alter_dim_user_level_add_discount.sql`：无前置依赖。
- `02_create_fact_user_daily_summary.sql`：无直接跨文件依赖，但逻辑上依赖 `dim_user_level` 和 `dim_date` 等已有表。
- `03_create_view_vw_user_level_revenue.sql`：依赖 `01` 修改后的 `dim_user_level` 表（新增 `discount` 字段），以及 `02` 创建的事实表（若视图引用该表）。

**依赖图结论：** 无循环依赖，可安全按上述顺序执行。

## 审核结论与建议

**建议：需整改后发版**

**整改项：**
1. **字段命名不一致**：变更说明中字段名为 `discount_rate`，但脚本实际新增字段名为 `discount`。请确认统一命名，并同步更新变更说明或脚本。
2. **补充变更说明**：在变更说明中补充 `fact_user_daily_summary` 表上三个索引的创建说明，确保文档与实现完全一致。

**风险提示：** 全表 UPDATE 操作虽已说明，但建议在非生产环境先行验证数据量级和锁影响，并考虑在业务低峰期执行。

**整改完成后，可重新审核并判定为 PASS。**