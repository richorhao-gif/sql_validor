# SQL 脚本语法与质量分析报告

| 字段 | 值 |
|---|---|
| 生成时间 | 2026-05-06 10:39:24 |
| 分析文件数 | 3 |
| 综合质量评分 | 🔴 **55 / 100** |
| ERROR 数 | 5 |
| WARNING 数 | 2 |
| INFO 数 | 4 |

---

## 各文件质量概览

| 文件名 | ERROR | WARNING | INFO | 主要问题摘要 |
|---|---|---|---|---|
| `01_sauce_mat_brd_prdct_atribt_redeploy.sql` | 5 | 3 | 4 | COMMENT ON VIEW 语法错误（5处）、恒真条件导致逻辑失效（2处）、CREATE TABLE 缺少 IF NOT EXISTS（3处）、ODS 表缺少 COMMENT ON 部分列 |
| `02_sauce_mat_abbr_add_column.sql` | 4 | 0 | 2 | COMMENT ON VIEW 语法错误（4处）、ODS 表缺少 COMMENT ON 部分列 |
| `03_sauce_mat_variety_new_asset.sql` | 4 | 2 | 4 | COMMENT ON VIEW 语法错误（4处）、恒真条件导致逻辑失效（2处）、CREATE TABLE 缺少 IF NOT EXISTS（3处）、ODS 表缺少 COMMENT ON 部分列 |

---

## 🔴 ERROR 级问题（5 项）

### E1 · `01_sauce_mat_brd_prdct_atribt_redeploy.sql` — SYNTAX_ERROR

**规则：** `SYNTAX_ERROR`  
**严重度：** ERROR  
**描述：** COMMENT ON VIEW 语句缺少 IS 关键字和注释内容，导致语法错误。静态工具已发现此问题，但未指出具体位置。共5处：ods.v_ods_mannual_sauce_mat_brd_prdct_atribt、dwd.v_dwd_scd_sauce_mat_brd_prdct_atribt、da.v_da_td_scd_sauce_mat_brd_prdct_atribt、da_selfhp.da_td_scd_sauce_mat_brd_prdct_atribt_调味物料品种与产品属性、da_selfhp.v_sauce_material_带简称。

**涉及片段：**
```sql
COMMENT ON VIEW ods.v_ods_mannual_sauce_mat_brd_prdct_atribt IS '调味物料品种与产品属性-ODS视图';
```

**建议：** 补全 IS 关键字和注释内容，例如：COMMENT ON VIEW ods.v_ods_mannual_sauce_mat_brd_prdct_atribt IS '调味物料品种与产品属性-ODS视图';

### E2 · `01_sauce_mat_brd_prdct_atribt_redeploy.sql` — LOGIC_ERROR

**规则：** `LOGIC_ERROR`  
**严重度：** ERROR  
**描述：** DWD 和 DA 层视图的 WHERE 子句包含恒真条件 'TRUE'::text = 'TRUE'::text，导致 OR 后面的 EXISTS 子查询永远无法执行，反向增量逻辑完全失效。静态工具未发现此问题。

**涉及片段：**
```sql
WHERE 'TRUE'::text = 'TRUE'::text
   OR EXISTS (
        SELECT 1 FROM delta_records dr
        WHERE dr.plant_code = ods.plant_code AND dr.mat_code = ods.mat_code
   );
```

**建议：** 删除恒真条件，直接使用 EXISTS 逻辑：WHERE EXISTS (SELECT 1 FROM delta_records dr WHERE dr.plant_code = ods.plant_code AND dr.mat_code = ods.mat_code);

### E3 · `02_sauce_mat_abbr_add_column.sql` — SYNTAX_ERROR

**规则：** `SYNTAX_ERROR`  
**严重度：** ERROR  
**描述：** COMMENT ON VIEW 语句缺少 IS 关键字和注释内容，导致语法错误。共4处：ods.v_ods_mannual_sauce_mat_abbr、dwd.v_dwd_scd_sauce_mat_abbr、da.v_da_sauce_material_full、da_selfhp.v_sauce_material_带简称。静态工具已发现此问题。

**涉及片段：**
```sql
COMMENT ON VIEW ods.v_ods_mannual_sauce_mat_abbr IS '调味物料简称-ODS视图（含短简称）';
```

**建议：** 补全 IS 关键字和注释内容。

### E4 · `03_sauce_mat_variety_new_asset.sql` — SYNTAX_ERROR

**规则：** `SYNTAX_ERROR`  
**严重度：** ERROR  
**描述：** COMMENT ON VIEW 语句缺少 IS 关键字和注释内容，导致语法错误。共4处：ods.v_ods_mannual_sauce_mat_variety、dwd.v_dwd_scd_sauce_mat_variety、da.v_da_td_scd_sauce_mat_variety、da_selfhp.sauce_mat_variety_调味品品种。静态工具已发现此问题。

**涉及片段：**
```sql
COMMENT ON VIEW ods.v_ods_mannual_sauce_mat_variety IS '调味品品种-ODS视图';
```

**建议：** 补全 IS 关键字和注释内容。

### E5 · `03_sauce_mat_variety_new_asset.sql` — LOGIC_ERROR

**规则：** `LOGIC_ERROR`  
**严重度：** ERROR  
**描述：** DWD 和 DA 层视图的 WHERE 子句包含恒真条件 'TRUE'::text = 'TRUE'::text，导致 OR 后面的 EXISTS 子查询永远无法执行，反向增量逻辑完全失效。静态工具未发现此问题。

**涉及片段：**
```sql
WHERE 'TRUE'::text = 'TRUE'::text
   OR EXISTS (
        SELECT 1 FROM delta_records dr WHERE dr.variety_code = ods.variety_code
   );
```

**建议：** 删除恒真条件，直接使用 EXISTS 逻辑。


---

## 🟡 WARNING 级问题（2 项）

### W1 · `01_sauce_mat_brd_prdct_atribt_redeploy.sql` — CREATE_NO_IF_NOT_EXISTS

**规则：** `CREATE_NO_IF_NOT_EXISTS`  
**严重度：** WARNING  
**描述：** ODS、DWD、DA 层的 CREATE TABLE 语句未使用 IF NOT EXISTS。虽然前面有 DROP TABLE IF EXISTS，但若脚本中途中断后重跑，DROP 已执行但 CREATE 失败，再次执行时表已存在会报错。静态工具已发现此问题。

**涉及片段：**
```sql
CREATE TABLE ods.ods_mannual_sauce_mat_brd_prdct_atribt ( ... ) USING beam;
```

**建议：** 改为 CREATE TABLE IF NOT EXISTS 以提升幂等性。

### W2 · `03_sauce_mat_variety_new_asset.sql` — CREATE_NO_IF_NOT_EXISTS

**规则：** `CREATE_NO_IF_NOT_EXISTS`  
**严重度：** WARNING  
**描述：** ODS、DWD、DA 层的 CREATE TABLE 语句未使用 IF NOT EXISTS。虽然前面有 DROP TABLE IF EXISTS，但若脚本中途中断后重跑，DROP 已执行但 CREATE 失败，再次执行时表已存在会报错。静态工具已发现此问题。

**涉及片段：**
```sql
CREATE TABLE ods.ods_mannual_sauce_mat_variety ( ... ) USING beam;
```

**建议：** 改为 CREATE TABLE IF NOT EXISTS 以提升幂等性。


---

## 🔵 INFO 级问题（4 项）

| # | 文件 | 规则 | 描述 |
|---|---|---|---|
| 1 | `01_sauce_mat_brd_prdct_atribt_redeploy.sql` | `NO_COMMENT` | ODS 表 ods.ods_mannual_sauce_mat_brd_prdct_atribt 的 plant_code、plant_descrptn、mat… |
| 2 | `02_sauce_mat_abbr_add_column.sql` | `NO_COMMENT` | ODS 表 ods.ods_mannual_sauce_mat_abbr 的 plant_code、mat_code、mat_abbr、zdelta_time、… |
| 3 | `03_sauce_mat_variety_new_asset.sql` | `NO_COMMENT` | ODS 表 ods.ods_mannual_sauce_mat_variety 的 variety_name、variety_type、is_active、zd… |
| 4 | `03_sauce_mat_variety_new_asset.sql` | `MISSING_INDEX` | 品种表 da.da_td_scd_sauce_mat_variety 的 parent_code 列用于树型结构自引用，但未创建索引。随着数据量增长，递归查询性… |

---

## 专项分析

### 🛡️ 安全风险

| 风险类型 | 数量 | 详情 |
|---|---|---|
| 无 WHERE 的 DELETE | 0 | — |
| 无 WHERE 的 UPDATE | 0 | — |
| TRUNCATE 操作 | 0 | — |
| DROP 操作 | 0 | — |

### ⚡ 性能规范

| 问题类型 | 数量 | 详情 |
|---|---|---|
| SELECT * | 0 | — |

---

## 改进建议

1. 【严重】修复所有 COMMENT ON VIEW 语法错误：共13处，均缺少 IS 关键字和注释内容。这是语法错误，会导致脚本执行失败。
2. 【严重】删除 DWD 和 DA 层视图中的恒真条件 'TRUE'::text = 'TRUE'::text：共4处（文件1和文件3各2处），该条件导致反向增量逻辑完全失效，所有数据都会被无条件返回。
3. 【建议】为所有 CREATE TABLE 添加 IF NOT EXISTS：共6处（文件1的3个表、文件3的3个表），提升脚本幂等性，避免重跑时因表已存在而报错。
4. 【建议】为品种表 da.da_td_scd_sauce_mat_variety 的 parent_code 列创建索引：该列用于树型自引用，无索引时递归查询性能会严重下降。
5. 【建议】为 ODS 层表补充缺失的列注释：文件1的 ods.ods_mannual_sauce_mat_brd_prdct_atribt 缺少6个列的 COMMENT ON，文件2的 ods.ods_mannual_sauce_mat_abbr 缺少5个列的 COMMENT ON，文件3的 ods.ods_mannual_sauce_mat_variety 缺少4个列的 COMMENT ON。

---

*本报告由 SQL 变更校验智能体自动生成*