# SQL 脚本变更说明对比报告

| 项目 | 内容 |
|------|------|
| **报告生成时间** | 2026-05-02T20:40:18.860262 |
| **脚本文件数** | 3 |
| **总体判定** | ❌ FAIL |
| **风险等级** | 🔴 HIGH |

## 执行概要

本次发版涉及 3 个 SQL 脚本，旨在对调味物料数据链路进行重构与扩展：① 重建调味物料品种与产品属性全链路（新增 `is_rawmat` 和 `prdct_atribt` 列）；② 为调味物料简称表新增 `mat_abbr_short` 列；③ 创建调味品品种全新资产链路。经审核，变更说明与实际脚本存在多处严重不一致：脚本③声称“库中当前不存在同名对象”却执行了 DROP TABLE 操作，说明对象已存在且数据被删除；变更说明提及的“DS 层重建 FOREIGN TABLE”在脚本中未体现；多个视图重建（如 `da_selfhp.v_sauce_material_带简称`）未在变更说明中明确。综合判定为 **FAIL**，风险等级 **HIGH**，**禁止发版**，需整改后重新审核。

## 变更覆盖情况

| ✅ 已确认 | ❌ 遗漏 | ⚠️ 额外变更 |
|-----------|---------|-------------|
| ① 重建 `ods.ods_mannual_sauce_mat_brd_prdct_atribt`、`dwd.dwd_scd_sauce_mat_brd_prdct_atribt`、`da.da_td_scd_sauce_mat_brd_prdct_atribt` 及对应视图（DROP+CREATE） | 变更说明提到「DS 层重建 FOREIGN TABLE」，但脚本影响汇总中未列出任何 DS 层（外部表）的变更操作，仅在 ODS/DWD/DA 层有操作 | [01_sauce_mat_brd_prdct_atribt_redeploy.sql] 中执行了 `DROP VIEW da_selfhp.v_sauce_material_带简称` 和 `CREATE VIEW da_selfhp.v_sauce_material_带简称`，变更说明未明确提及该视图名称 |
| ① 影响下游视图 `da.v_da_sauce_material_full` 和 `da_selfhp` 相关视图重建 | — | [02_sauce_mat_abbr_add_column.sql] 中执行了 `DROP VIEW da_selfhp.v_sauce_material_带简称` 和 `CREATE VIEW da_selfhp.v_sauce_material_带简称`，变更说明未明确提及该视图名称 |
| ② 为 `ods.ods_mannual_sauce_mat_abbr`、`dwd.dwd_scd_sauce_mat_abbr`、`da.da_td_scd_sauce_mat_abbr` 新增 `mat_abbr_short` 列（ALTER TABLE），并重建相关视图 | — | [03_sauce_mat_variety_new_asset.sql] 中执行了 `DROP TABLE/CREATE TABLE` 操作（`ods.ods_mannual_sauce_mat_variety`、`dwd.dwd_scd_sauce_mat_variety`、`da.da_td_scd_sauce_mat_variety`），但变更说明称「库中当前不存在同名对象」，实际脚本却执行了 DROP（说明对象已存在），存在描述不一致 |
| ③ 全新资产 `ods.ods_mannual_sauce_mat_variety`、`dwd.dwd_scd_sauce_mat_variety`、`da.da_td_scd_sauce_mat_variety` 及对应视图的 DROP+CREATE | — | — |

## 高风险操作清单

- 🔴 **[01_sauce_mat_brd_prdct_atribt_redeploy.sql]**: `DROP TABLE ods.ods_mannual_sauce_mat_brd_prdct_atribt`、`dwd.dwd_scd_sauce_mat_brd_prdct_atribt`、`da.da_td_scd_sauce_mat_brd_prdct_atribt` — 全量删除历史数据，变更说明已说明重建，但需确认数据备份与重新加载流程。
- 🔴 **[03_sauce_mat_variety_new_asset.sql]**: `DROP TABLE ods.ods_mannual_sauce_mat_variety`、`dwd.dwd_scd_sauce_mat_variety`、`da.da_td_scd_sauce_mat_variety` — 变更说明称「全新资产创建，库中当前不存在同名对象」，但脚本实际执行了 DROP，说明对象已存在，存在描述不一致风险，且数据被删除。
- 🔴 **[02_sauce_mat_abbr_add_column.sql]**: `ALTER TABLE` 新增字段 — 虽为增量变更，但变更说明未提及 DS 层 FOREIGN TABLE 的具体实现，存在遗漏。

## 数据库变更影响详情

### 新增对象列表

| 对象全名 | 类型 | 所属脚本 | 说明 |
|----------|------|----------|------|
| `ods.ods_mannual_sauce_mat_brd_prdct_atribt` | TABLE | 01 | 重建（DROP后CREATE），结构可能调整 |
| `dwd.dwd_scd_sauce_mat_brd_prdct_atribt` | TABLE | 01 | 重建 |
| `da.da_td_scd_sauce_mat_brd_prdct_atribt` | TABLE | 01 | 重建 |
| `ods.v_ods_mannual_sauce_mat_brd_prdct_atribt` | VIEW | 01 | 重建 |
| `dwd.v_dwd_scd_sauce_mat_brd_prdct_atribt` | VIEW | 01 | 重建 |
| `da.v_da_td_scd_sauce_mat_brd_prdct_atribt` | VIEW | 01 | 重建 |
| `da.v_da_sauce_material_full` | VIEW | 01 | 重建 |
| `da_selfhp.da_td_scd_sauce_mat_brd_prdct_atribt_调味物料品种与产品属性` | VIEW | 01 | 重建 |
| `da_selfhp.v_sauce_material_带简称` | VIEW | 01 | 重建 |
| `ods.ods_mannual_sauce_mat_variety` | TABLE | 03 | 重建（声称全新但实际已存在） |
| `dwd.dwd_scd_sauce_mat_variety` | TABLE | 03 | 重建 |
| `da.da_td_scd_sauce_mat_variety` | TABLE | 03 | 重建 |
| `ods.v_ods_mannual_sauce_mat_variety` | VIEW | 03 | 新建（脚本中未列出具体名称，根据模式推断） |
| `dwd.v_dwd_scd_sauce_mat_variety` | VIEW | 03 | 新建 |
| `da.v_da_td_scd_sauce_mat_variety` | VIEW | 03 | 新建 |
| `ods.v_ods_mannual_sauce_mat_abbr` | VIEW | 02 | 重建（DROP后CREATE） |
| `da_selfhp.v_sauce_material_带简称` | VIEW | 02 | 重建 |

### 修改对象列表

| 对象全名 | 类型 | 所属脚本 | 修改内容 |
|----------|------|----------|----------|
| `ods.ods_mannual_sauce_mat_abbr` | TABLE | 02 | ALTER TABLE 新增 `mat_abbr_short` 列 |
| `dwd.dwd_scd_sauce_mat_abbr` | TABLE | 02 | ALTER TABLE 新增 `mat_abbr_short` 列 |
| `da.da_td_scd_sauce_mat_abbr` | TABLE | 02 | ALTER TABLE 新增 `mat_abbr_short` 列 |

### 删除对象列表

| 对象全名 | 类型 | 所属脚本 | 说明 |
|----------|------|----------|------|
| `ods.ods_mannual_sauce_mat_brd_prdct_atribt` | TABLE | 01 | 被DROP（后重建） |
| `dwd.dwd_scd_sauce_mat_brd_prdct_atribt` | TABLE | 01 | 被DROP（后重建） |
| `da.da_td_scd_sauce_mat_brd_prdct_atribt` | TABLE | 01 | 被DROP（后重建） |
| `ods.v_ods_mannual_sauce_mat_brd_prdct_atribt` | VIEW | 01 | 被DROP（后重建） |
| `dwd.v_dwd_scd_sauce_mat_brd_prdct_atribt` | VIEW | 01 | 被DROP（后重建） |
| `da.v_da_td_scd_sauce_mat_brd_prdct_atribt` | VIEW | 01 | 被DROP（后重建） |
| `da.v_da_sauce_material_full` | VIEW | 01 | 被DROP（后重建） |
| `da_selfhp.da_td_scd_sauce_mat_brd_prdct_atribt_调味物料品种与产品属性` | VIEW | 01 | 被DROP（后重建） |
| `da_selfhp.v_sauce_material_带简称` | VIEW | 01 | 被DROP（后重建） |
| `ods.ods_mannual_sauce_mat_variety` | TABLE | 03 | 被DROP（后重建） |
| `dwd.dwd_scd_sauce_mat_variety` | TABLE | 03 | 被DROP（后重建） |
| `da.da_td_scd_sauce_mat_variety` | TABLE | 03 | 被DROP（后重建） |
| `ods.v_ods_mannual_sauce_mat_abbr` | VIEW | 02 | 被DROP（后重建） |
| `da.v_da_sauce_material_full` | VIEW | 02 | 被DROP（后重建） |
| `da_selfhp.v_sauce_material_带简称` | VIEW | 02 | 被DROP（后重建） |

### 数据变更影响

- **全量数据删除（高风险）**：
  - `ods.ods_mannual_sauce_mat_brd_prdct_atribt`、`dwd.dwd_scd_sauce_mat_brd_prdct_atribt`、`da.da_td_scd_sauce_mat_brd_prdct_atribt` — 历史数据全部丢失，需重新加载。
  - `ods.ods_mannual_sauce_mat_variety`、`dwd.dwd_scd_sauce_mat_variety`、`da.da_td_scd_sauce_mat_variety` — 历史数据全部丢失（变更说明声称全新资产，但实际已存在数据）。
- **增量数据变更**：
  - `ods.ods_mannual_sauce_mat_abbr`、`dwd.dwd_scd_sauce_mat_abbr`、`da.da_td_scd_sauce_mat_abbr` — 新增 `mat_abbr_short` 列，已有数据保留，新字段为空，需后续填充。

## 执行顺序与依赖关系说明

1. **脚本执行顺序**：必须严格按照 `01 → 02 → 03` 的顺序执行。
   - **01** 重建 `ods_mannual_sauce_mat_brd_prdct_atribt` 及其下游视图，包括 `da.v_da_sauce_material_full` 和 `da_selfhp.v_sauce_material_带简称`。
   - **02** 对 `ods_mannual_sauce_mat_abbr` 等表执行 ALTER 新增列，并重建依赖的视图（`da.v_da_sauce_material_full` 和 `da_selfhp.v_sauce_material_带简称`）。注意：02 脚本中再次 DROP/CREATE 了 `da_selfhp.v_sauce_material_带简称`，该视图在 01 中已被重建，因此 02 必须晚于 01 执行，否则 01 重建的视图会被 02 覆盖。
   - **03** 创建全新资产 `sauce_mat_variety` 链路，不依赖前两个脚本，但为避免命名冲突或依赖混乱，建议最后执行。
2. **跨文件依赖**：`da.v_da_sauce_material_full` 视图同时依赖 `da.da_td_scd_sauce_mat_brd_prdct_atribt`（01 脚本）和 `da.da_td_scd_sauce_mat_abbr`（02 脚本），因此 01 和 02 均需执行完成后该视图才能正确创建。当前 01 和 02 均重建了该视图，但 02 重建时依赖的表（`da.da_td_scd_sauce_mat_abbr`）已新增列，结构一致，无冲突。
3. **风险提示**：若执行顺序颠倒（如先执行 02 再执行 01），则 01 会重建 `da.v_da_sauce_material_full` 和 `da_selfhp.v_sauce_material_带简称`，导致 02 中的 ALTER 操作可能因视图依赖而失败（需先删除视图）。建议严格按顺序执行，并在事务中做好回滚预案。

## 审核结论与建议

**结论：❌ 禁止发版**

**建议**：
1. **修正变更说明**：脚本③的描述必须更正，明确说明对象已存在且将执行 DROP+CREATE 重建，并评估数据丢失风险。
2. **补充 DS 层操作**：若确实需要重建 DS 层 FOREIGN TABLE，请补充对应脚本或说明；若不需要，则删除变更说明中的相关描述。
3. **补充遗漏视图**：在变更说明中明确列出所有受影响的视图，特别是 `da_selfhp.v_sauce_material_带简称` 的重建。
4. **数据备份与恢复**：对于所有 DROP TABLE 操作，需确认已有完整数据备份，并制定重新加载流程（尤其是脚本③中声称“全新资产”但实际已存在数据的表）。
5. **重新审核**：完成上述整改后，重新提交变更说明与脚本进行审核，确保描述与实现完全一致。