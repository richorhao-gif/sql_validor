# SQL 脚本变更说明对比报告

| 字段 | 值 |
|:---|:---|
| 📅 生成时间 | 2026-05-08 09:13:51 |
| 📁 SQL 文件数 | 2 |
| 🏁 总体判定 | ❌ **FAIL** |
| 🔴 风险等级 | **CRITICAL** |
| 🤖 LLM 置信度 | 95% |

---

> **推荐执行顺序：** `20260428_聂晓勇_coding#5775_工厂物料成本新增字段-1对象.SQL` → `20260428_聂晓勇_coding#5775_工厂物料成本新增字段-2刷数.SQL`

## 本次发版人员概览

| 开发者 | 文件数 | 文件名 | ✅ 无问题 | ❌ 有问题 |
|:---|:---:|:---|:---:|:---:|
| 未知开发者 | 2 | `20260428_聂晓勇_coding#5775_工厂物料成本新增字段-1对象.SQL`<br>`20260428_聂晓勇_coding#5775_工厂物料成本新增字段-2刷数.SQL` | 0 | 2 |
| **合计** | **2** | — | **0** | **2** |

> 本次发版共 **2** 个文件，涉及 **1** 位开发者。✅ 0 个文件无问题，❌ 2 个文件有问题。

---

## 审核结论

> [!CAUTION]
> **❌ 审核结论：存在阻断性风险，暂不建议发版**
>
> 脚本存在 2 处 COMMENT ON 语法错误（CRITICAL），且 ALTER ADD COLUMN 的目标列已存在（HIGH），脚本执行将失败

## 执行概要

本次发版涉及工厂物料成本（CKM3实际成本明细）相关数据链路的新增字段改造。文件1负责：① 给 DWD 和 DA 层底层表新增 `mrkt_price` 市场价字段；② 重建 DWD→DA→DA_SELFHP 三层视图，关联 SAP 市场价源表 `ods_sap_zac_cst_mktprice` 透出市场价指标。文件2为刷数脚本，调用系统存储过程同步新字段。执行顺序合理（文件1→文件2），无循环依赖。但脚本中存在 **2 处 CRITICAL 语法错误**（COMMENT ON 缺少视图名）和 **2 处 HIGH 风险**（ALTER ADD COLUMN 目标列已存在），脚本无法正常执行。

---

## 发版人员详情

### 👤 未知开发者（2 个文件）

#### 📄 `20260428_聂晓勇_coding#5775_工厂物料成本新增字段-1对象.SQL` — ❌ 有问题

| # | 严重度 | 类别 | 描述 |
|:---:|:---:|:---|:---|
| 1 | 🔴 CRITICAL | MISMATCH | COMMENT ON VIEW 语法错误 — 缺少视图对象名。脚本中存在 `COMMENT ON VIEW dwd IS` 语句，缺少视图名（应为 `COMMENT ON VIEW dwd.v_dwd… |
| 2 | 🔴 CRITICAL | MISMATCH | COMMENT ON VIEW 语法错误 — 缺少视图对象名。脚本中存在 `COMMENT ON VIEW da IS` 语句，缺少视图名（应为 `COMMENT ON VIEW da.v_da_td… |
| 3 | 🟠 HIGH | MISMATCH | ALTER TABLE ADD COLUMN 目标列已存在 — dwd 表。`ALTER TABLE dwd.dwd_plant_mat_cost_ckm3_actl_cost_detl ADD CO… |
| 4 | 🟠 HIGH | MISMATCH | ALTER TABLE ADD COLUMN 目标列已存在 — da 表。`ALTER TABLE da.da_td_plant_mat_cost_ckm3_actl_cost_detl ADD CO… |

**F1 · [CRITICAL] MISMATCH**

COMMENT ON VIEW 语法错误 — 缺少视图对象名。脚本中存在 `COMMENT ON VIEW dwd IS` 语句，缺少视图名（应为 `COMMENT ON VIEW dwd.v_dwd_plant_mat_cost_ckm3_actl_cost_detl IS '...'`），仅写了 schema 名。解析器报错：`Required keyword: 'expression' missing for Comment`。

> [!TIP]
> **建议：** 补充完整的视图全限定名和注释内容。

**F2 · [CRITICAL] MISMATCH**

COMMENT ON VIEW 语法错误 — 缺少视图对象名。脚本中存在 `COMMENT ON VIEW da IS` 语句，缺少视图名（应为 `COMMENT ON VIEW da.v_da_td_plant_mat_cost_ckm3_actl_cost_detl IS '...'`），仅写了 schema 名。

> [!TIP]
> **建议：** 补充完整的视图全限定名和注释内容。

**F3 · [HIGH] MISMATCH**

ALTER TABLE ADD COLUMN 目标列已存在 — dwd 表。`ALTER TABLE dwd.dwd_plant_mat_cost_ckm3_actl_cost_detl ADD COLUMN mrkt_price DECIMAL(13,3) NULL`，但数据库中该列已存在（ordinal 19, 类型 DECIMAL(13,3), 可空）。重复 ADD 将导致脚本执行中断。

> [!TIP]
> **建议：** 使用 `ALTER TABLE ... ADD COLUMN IF NOT EXISTS` 语法，或确认列已存在后移除该 ALTER 语句。

**F4 · [HIGH] MISMATCH**

ALTER TABLE ADD COLUMN 目标列已存在 — da 表。`ALTER TABLE da.da_td_plant_mat_cost_ckm3_actl_cost_detl ADD COLUMN mrkt_price DECIMAL(13,3) NULL`，但数据库中该列已存在（ordinal 19, 类型 DECIMAL(13,3), 可空）。重复 ADD 将导致脚本执行中断。

> [!TIP]
> **建议：** 使用 `ALTER TABLE ... ADD COLUMN IF NOT EXISTS` 语法，或确认列已存在后移除该 ALTER 语句。

#### 📄 `20260428_聂晓勇_coding#5775_工厂物料成本新增字段-2刷数.SQL` — ❌ 有问题

| # | 严重度 | 类别 | 描述 |
|:---:|:---:|:---|:---|
| 1 | 🟡 MEDIUM | MISMATCH | 刷数脚本内容未解析。该文件未解析出任何 DDL/DML 语句，变更说明提到 |

**F1 · [MEDIUM] MISMATCH**

刷数脚本内容未解析。该文件未解析出任何 DDL/DML 语句，变更说明提到

> [!TIP]
> **建议：** 人工复核刷数脚本中的存储过程调用语句。


---

## 变更覆盖情况

| 类别 | 数量 |
|:---|:---:|
| ✅ 已确认 | 6 |
| ❌ 遗漏 | 0 |
| ⚠️ 额外变更 | 0 |

### ✅ 已确认（6 项）

| # | 变更说明要点 | 对应实现 |
|:---:|:---|:---|
| 1 | DWD 底层表新增市场价字段 | ALTER TABLE dwd.dwd_plant_mat_cost_ckm3_actl_cost_detl ADD COLUMN mrkt_price DECIMAL(13,3) NULL |
| 2 | DA 底层表新增市场价字段 | ALTER TABLE da.da_td_plant_mat_cost_ckm3_actl_cost_detl ADD COLUMN mrkt_price DECIMAL(13,3) NULL |
| 3 | 重建 DWD 层视图，关联 SAP 市场价源表 | DROP VIEW + CREATE OR REPLACE VIEW dwd.v_dwd_plant_mat_cost_ckm3_actl_cost_detl（新增关联 ods.ods_sap_zac_cst_mktprice） |
| 4 | 重建 DA 层视图 | DROP VIEW + CREATE OR REPLACE VIEW da.v_da_td_plant_mat_cost_ckm3_actl_cost_detl |
| 5 | 重建 BI 自助层中文视图 | DROP VIEW + CREATE OR REPLACE VIEW da_selfhp.da_td_plant_mat_cost_ckm3_actl_cost_detl_工厂物料成本（ckm3实际成本明细） |
| 6 | 刷数脚本同步新字段 | 调用系统存储过程（未解析出具体对象） |

### ❌ 遗漏（0 项）

_无遗漏项。_

### ⚠️ 额外变更（0 项）

_无额外变更。_

---

## 🔴 高风险操作清单

| # | 操作描述 |
|:---:|:---|
| 1 | ALTER TABLE dwd.dwd_plant_mat_cost_ckm3_actl_cost_detl ADD COLUMN mrkt_price：该列已在数据库中存在（ordinal 19, DECIMAL(13,3), NULL），重复 ADD 将导致脚本执行失败。建议改为 `ADD COLUMN IF NOT EXISTS` 或确认列已存在后移除该语句。 |
| 2 | ALTER TABLE da.da_td_plant_mat_cost_ckm3_actl_cost_detl ADD COLUMN mrkt_price：同上，该列已在数据库中存在（ordinal 19, DECIMAL(13,3), NULL），重复 ADD 将导致脚本执行失败。 |

---

## 数据库变更影响详情

| 类别 | 数量 |
|:---|:---:|
| 🆕 新增对象 | 3 |
| ✏️ 修改对象 | 2 |
| 🗑️ 删除对象 | 3 |
| 📝 DML 操作 | 0 |

### 🆕 新增对象（3 个）

| 对象类型 | Schema | 对象名 | 所在文件 | 备注 |
|:---|:---|:---|:---|:---|
| `VIEW` | `dwd` | `v_dwd_plant_mat_cost_ckm3_actl_cost_detl` | `文件1` | CREATE OR REPLACE，重建（新增关联 ods_sap_zac_cst_mktprice） |
| `VIEW` | `da` | `v_da_td_plant_mat_cost_ckm3_actl_cost_detl` | `文件1` | CREATE OR REPLACE，重建（透出 mrkt_price） |
| `VIEW` | `da_selfhp` | `da_td_plant_mat_cost_ckm3_actl_cost_detl_工厂物料成本（ckm3实际成本明细）` | `文件1` | CREATE OR REPLACE，重建（security_invoker=TRUE，透出 mrkt_price） |

### ✏️ 修改对象（2 个）

| 对象类型 | Schema | 对象名 | 所在文件 | 备注 |
|:---|:---|:---|:---|:---|
| `TABLE` | `dwd` | `dwd_plant_mat_cost_ckm3_actl_cost_detl` | `文件1` | ADD COLUMN mrkt_price（⚠️ 列已存在） |
| `TABLE` | `da` | `da_td_plant_mat_cost_ckm3_actl_cost_detl` | `文件1` | ADD COLUMN mrkt_price（⚠️ 列已存在） |

### 🗑️ 删除对象（3 个）

| 对象类型 | Schema | 对象名 | 所在文件 | 备注 |
|:---|:---|:---|:---|:---|
| `VIEW` | `da_selfhp` | `da_td_plant_mat_cost_ckm3_actl_cost_detl_工厂物料成本（ckm3实际成本明细）` | `文件1` | DROP VIEW IF EXISTS，后续重建 |
| `VIEW` | `da` | `v_da_td_plant_mat_cost_ckm3_actl_cost_detl` | `文件1` | DROP VIEW IF EXISTS，后续重建 |
| `VIEW` | `dwd` | `v_dwd_plant_mat_cost_ckm3_actl_cost_detl` | `文件1` | DROP VIEW IF EXISTS，后续重建 |

### 📝 DML 数据影响

_无 DML 操作。_

---

<sub>🤖 本报告由 SQL 变更校验智能体自动生成</sub>